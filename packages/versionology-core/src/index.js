// @flow

/**
 * This function says hello.
 * @param {string} name Some name to say hello for.
 * @return {string} The hello.
 */
const sayHello = (name: string = 'Nunnery'): string => `Hello, ${name}!`;

export default sayHello;
